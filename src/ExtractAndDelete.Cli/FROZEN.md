# CLI source frozen

`ExtractAndDelete.Cli` is retained as historical source only. Starting with V4 it is intentionally excluded from the solution graph, default restore/build/test/publish scripts, acceptance checks, and user documentation.

Do not add new features or treat the project as a supported delivery entry point. Existing binaries and build folders are not removed automatically by V4 scripts.
